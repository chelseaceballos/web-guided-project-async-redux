const initialState = {
    editing: false,
    title: 'WEBPT28 Thunky Reducer fiesta'
};

export const titleReducer = (state = initialState, action) => {
    switch (action.type) {
        case default:
            return state;
    }
}